// Main schema file - generator and datasource config
// Models are organized in separate .prisma files in this directory

generator client {
  provider = "prisma-client"
  output   = "../lib/generated/prisma"
}

datasource db {
  provider = "postgresql"
}
